# Bootstrap only - app settings (API keys, Databricks, retrieval, IoT scoring)
# are managed in Admin → Settings after login.

DATABASE_URL=postgresql://infraops:infraops@localhost:5432/infraops
REDIS_URL=redis://localhost:6379
JWT_SECRET=change-me-in-production-use-a-long-random-string
NODE_ENV=development
PORT=3000
WEB_ORIGIN=http://localhost:5173

# Optional bootstrap overrides (prefer Admin → Settings in running app):
# IOT_SCORING_BACKEND=heuristic   # heuristic | model_serving
# IOT_MODEL_ENDPOINT_URL=
# IOT_MODEL_ENDPOINT_TOKEN=
